<%= search_form_for @q do |f| %>
    <div class="row justify-content-end">
        <div class="form-group col-3">
            <%= f.label "" %>
            <%= f.collection_select(:psychoative_substances_name_cont, PsychoativeSubstance.all.order(:name), :name, :name, {include_blank: 'Substância Psicoativas'}, class:'form-control') %>
        </div>

        <div class="form-group col-2">
            <%= f.label "" %>
            <%= f.collection_select(:cid10s_code_cont, Cid10.all.order(:code), :code, :code, {include_blank: 'CID 10'}, class: 'form-control') %>
        </div>

        <div class="form-group col-2">
            <%= f.label "" %>
            <%= f.collection_select(:cnes_unity_cont, Cnes.all.order(:unity), :unity, :unity, {include_blank: 'Unidade'}, class: 'form-control') %>
        </div>

        <div class="form-group col-3">
            <%= f.label "" %>
            <%= f.search_field :name_cont, class: 'form-control', placeholder: 'Nome do paciente' %>
        </div> 

        <div class="form-group pt-4 mr-3">
            <%= f.submit 'Buscar', class: 'btn btn-light border'  %>
        </div>
    </div>
<% end %>